import React, { Component } from "react";

export default class cookies extends Component {
  render() {
    return (
      <div>
        The FUB assumes that every visitor to the corporate website of the Bages
        University Foundation accepts the use of cookies
      </div>
    );
  }
}
